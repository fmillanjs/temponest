{
  "name": "@temponest/auth",
  "version": "0.1.0",
  "private": true,
  "description": "Better Auth configuration and utilities",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "clean": "rm -rf .turbo node_modules dist"
  },
  "dependencies": {
    "@temponest/database": "workspace:*",
    "better-auth": "^0.6.0",
    "better-call": "^0.2.0"
  },
  "devDependencies": {
    "@temponest/typescript-config": "workspace:*",
    "typescript": "^5.3.3"
  }
}
